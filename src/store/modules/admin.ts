import {Module} from 'vuex'
import {State} from '../index'

export interface AdminState {

}

export const system:Module<AdminState,State> = {
    state:()=>({

    }),
    mutations:{
        
    }
}