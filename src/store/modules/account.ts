import {Module} from 'vuex'
import {State} from '../index'

export interface AccountState {

}

export const system:Module<AccountState,State> = {
    state:()=>({

    }),
    mutations:{
        
    }
}